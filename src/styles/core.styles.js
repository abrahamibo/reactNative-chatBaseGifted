import colors from "./core/colors.styles";
import fonts from "./core/fonts.styles";
import mixins from "./core/mixins.styles";

const core = {
    colors: colors,
    fonts: fonts,
    mixins: mixins
}

export default core
